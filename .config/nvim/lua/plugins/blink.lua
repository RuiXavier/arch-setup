return {
  {
    "saghen/blink.cmp",
    opts = {
      completion = {
        -- This stops the giant preview window from jumpscaring you
        documentation = { auto_show = false },

        -- Optional: This stops the grey "ghost text" from appearing inline
        ghost_text = { enabled = false },
      },
    },
  },
}
